# SPYDER — runtime dependencies (Python 3.12+)
# Install all:   pip install -r requirements.txt
# Editable install (recommended for development):  pip install -e .

# ── Core runtime ─────────────────────────────────────────────────────────────
httpx[http2]>=0.27
rich>=13.7
textual>=0.58
prompt_toolkit>=3.0
pydantic>=2.6
pydantic-settings>=2.2
jinja2>=3.1
pyyaml>=6.0
beautifulsoup4>=4.12

# ── Optional speedups (uncomment to enable) ──────────────────────────────────
# uvloop>=0.19 ; sys_platform != "win32"
# orjson>=3.10
# aiofiles>=23.2

# ── Optional: PDF reports ─────────────────────────────────────────────────────
# weasyprint>=61.0

# ── Development / testing ─────────────────────────────────────────────────────
# pytest>=8.0
# pytest-asyncio>=0.23
# ruff>=0.4
# respx>=0.21
# mypy>=1.9
